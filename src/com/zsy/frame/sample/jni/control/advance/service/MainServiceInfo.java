package com.zsy.frame.sample.jni.control.advance.service;

/**
 * @description：IPC通信概述
 * 一般而言，一个Activity应用程序里面的各组件（如Activity，Service等）都市同一个进程里面执行；
 * 这种在同一进程内的通信，又称为短程通信，意味着，两个Activity在同一个进程（Process）里面执行；
 * 相对而言，远程（Remote）通信的意思是：两个组件（如Activity或Service）分别在不同的进程里执行；
 * 两者之间是IPC通信，又称远程通信；
 * 
 * 4、IPC的IBinder接口---定义与实现
 * 
 * @author samy
 * @date 2015-3-24 下午9:12:00
 */
public class MainServiceInfo {

}
